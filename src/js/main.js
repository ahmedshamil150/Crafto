// src/js/main.js
// Basic UI helpers – for now just a placeholder
document.addEventListener('DOMContentLoaded', () => {
  console.log('Crafto front‑end loaded');
  // Future: toggle mobile menu, init cart from localStorage, etc.
});
